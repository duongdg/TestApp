<li>
  <%= gravatar_for user, size: Settings.us_index.size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && current_user.present? %>
    | <%= link_to t(".delete"), user, method: :delete,
        data: {confirm: t("sure")} %>
  <% end %>
</li>
